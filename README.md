# ML Tutor

Project scaffold for ML Tutor.

Structure:

- frontend/: React frontend (src, components, services, hooks, store)
- backend/: FastAPI backend (app, db, models, routes, services)

Run suggestions:

- Backend: `uvicorn backend.app.main:app --reload`
- Frontend: standard React start (`npm start` / `yarn start`)
